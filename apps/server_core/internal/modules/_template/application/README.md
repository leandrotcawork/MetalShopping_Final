# application

Place use cases, commands, queries, and orchestration of domain behavior here.

This layer coordinates domain logic through explicit ports and should not become a second domain model.

