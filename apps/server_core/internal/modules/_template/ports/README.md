# ports

Place explicit module interfaces and dependency contracts here.

This layer defines what the module needs from infrastructure or neighboring capabilities, not how those needs are implemented.

