{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.metalshopping.local/api/shopping_supplier_signal_v1.schema.json",
  "title": "ShoppingSupplierSignalV1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "productId",
    "supplierCode",
    "urlStatus",
    "lookupMode",
    "lookupModeSource",
    "manualOverride",
    "updatedAt"
  ],
  "properties": {
    "productId": {
      "type": "string",
      "minLength": 1
    },
    "supplierCode": {
      "type": "string",
      "minLength": 1
    },
    "productUrl": {
      "type": ["string", "null"]
    },
    "urlStatus": {
      "type": "string",
      "enum": ["ACTIVE", "STALE", "INVALID"]
    },
    "lookupMode": {
      "type": "string",
      "enum": ["EAN", "REFERENCE"]
    },
    "lookupModeSource": {
      "type": "string",
      "enum": ["INFERRED", "MANUAL"]
    },
    "manualOverride": {
      "type": "boolean"
    },
    "lastCheckedAt": {
      "type": ["string", "null"]
    },
    "lastSuccessAt": {
      "type": ["string", "null"]
    },
    "lastHttpStatus": {
      "type": ["integer", "null"]
    },
    "lastErrorMessage": {
      "type": ["string", "null"]
    },
    "nextDiscoveryAt": {
      "type": ["string", "null"],
      "format": "date-time"
    },
    "notFoundCount": {
      "type": "integer",
      "minimum": 0
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    }
  }
}
