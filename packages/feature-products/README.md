# feature-products

Feature-local adapters, view models, and UI composition for the `Products` surface.

This package may derive view models from generated DTOs, but must not redefine canonical contracts.
