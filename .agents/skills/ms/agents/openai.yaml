interface:
  display_name: "MetalShopping"
  short_description: "Master orchestrator. Plan + implement + review any feature end-to-end. Enters plan mode, validates architecture, orchestrates all skills. Usage: 'use $ms to implement X'."
  default_prompt: "Use $ms to implement the requested feature."
policy:
  allow_implicit_invocation: true
