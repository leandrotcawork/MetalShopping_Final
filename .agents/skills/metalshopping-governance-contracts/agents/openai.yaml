interface:
  display_name: "MS Governance"
  short_description: "Create or review governance contracts (policies, thresholds, feature flags). Runtime hierarchy, scope resolution, auditable artifacts."
  default_prompt: "Use $metalshopping-governance-contracts to create or review the governance artifact."
policy:
  allow_implicit_invocation: true
