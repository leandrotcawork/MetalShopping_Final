interface:
  display_name: "MS Events"
  short_description: "Create or review versioned event contracts under contracts/events/v1. Naming, envelope, idempotency key, payload schema."
  default_prompt: "Use $metalshopping-event-contracts to create or review the event contract."
policy:
  allow_implicit_invocation: true
