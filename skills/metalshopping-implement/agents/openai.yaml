interface:
  display_name: "MS Implement"
  short_description: "Implement Go module or Python worker with real repo patterns. BeginTenantTx, AppendInTx, handler auth flow, worker tenant context."
  default_prompt: "Use $metalshopping-implement to implement the current task."
policy:
  allow_implicit_invocation: true
